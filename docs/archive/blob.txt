Final task for today: Living Documentation and Validation.

Audit the Spec: Read the current POLLY_PROTOCOL.md and compare it against the logic you just implemented (Merkle utility, did_method claims, and signature-strict voting).

Update: Rewrite the spec to reflect the actual implementation details. Ensure the API table matches our new REST endpoints and that the 'Security' section accurately describes our Merkle anchoring process.

Update POLLY_DEVELOPER_GUIDE.md: Document the (updated) Polly Protocol Spec v1 implementation, specifically the Merkle root anchoring and the signature-strict voting requirements.

Basic Testing: Add a test in apps/poller/tests.py that attempts to cast a vote without a signature and confirms it is rejected, and another test that validates the merkle.py utility correctly hashes a list of 10 signatures into a single root.

Validation: Ensure that any developer reading the spec tomorrow can precisely replicate our cryptographic voting flow.